# AI 판단기준 변경 이력 (프롬프트 재작성 시 반드시 유지할 결정들)

프롬프트를 다시 쓸 때 이 목록의 결정이 빠지면 과거에 실제 사례로 잡았던 오류가 되살아남.
각 항목은 "왜 그렇게 정했는지"까지 같이 적음. (커밋 해시는 git log 참고)

## 대원칙 (2026-09 재정립)
- 목적은 정확한 지급. 돈을 안 주려는 도구가 아님. 근거 없는 삭감과 근거 없는 인정은 같은 무게의 오류.
- AI는 규칙표 대조기가 아니라 판단 주체. 프롬프트는 "무엇을 보고, 어떤 순서로 생각하고, 어떤 형식으로 답할지"만 정하고 결론을 미리 정해주는 절대규칙은 두지 않음.
- 판단 순서: 손상 관찰 → 정비사 관점 복원 가능 여부 → 불가면 교환 / 가능하면 수리 수준(유형·판금 등급·도장 등급) → 복원은 되나 비용이 교환에 근접하면 협의 + 비용 비교 → 부수·중복·도장·시간 검토.
  - 이전 구조(도장막 상태로 1~3유형을 먼저 매기고 교환은 "3유형이라도 예외")가 도장 멀쩡한 꺾임(펜더 가장자리 크리스 + 램프 접합부 단차)을 2유형으로 내린 사례가 계기.
- 회사 수치(시간표·한도·허용목록)는 [참고자료]로 들어올 때만 적용. 프롬프트에 박지 않음.
- 건 전체 순서 (v4.1/adj3.1): ① 파손 검토 → 필요 메인·부수작업(명확한 범위) → ② 청구 대조 + 작업사진 검증(직접확인) → ③ 범위 밖 청구는 파손 형태·충격 방향으로 파손 가능성 추론(인정/협의/불인정, 모두 "추론" 표기) → ④ 내부 기준으로 시간·수량·도장 등급 대조(협의·과다) → ⑤ 직접확인/추론 구분 표기.
  - 계기: 펜더 교환 건에 휠하우스·사이드멤버·프론트필러 판금이 작업사진만으로 전부 인정으로 나옴. 작업사진은 "했다"의 증명이지 "필요했다"의 증명이 아님(뜯으면 다 주는 구조 금지). 휠하우스는 개연성 충분, 사이드멤버는 충격 방향상 애매, 필러 힌지부는 도어 단차 근거 없으면 협의.
  - 출력: judgment_basis("직접확인"|"추론") 필드, 화면 "추론" 배지 + 실험 표 "추론만" 필터, 복사 텍스트 [추론].

## 교환 vs 수리
- 교환 판정 3구간 (v3.4/adj2.5): 복원 곤란 근거 있으면 인정 / 완만한 함몰·긁힘에 교환이면 과다청구 / 사이는 협의필요 + cost_comparison(교환안 vs 수리안). 금액은 청구서 인쇄 금액과 청구서 단가 환산치만(AI 자체 산정 금지).
- 외판 교환은 손상 기준으로 결론 (v3.5/adj2.6): 손상·탈거 사진이 있으면 "신품 장착 사진 없음"으로 불인정 금지. reasoning 첫 문장은 "판금이 맞다 / 교환이 맞다".
- 범퍼(PP): 표면 균열 선은 소재 파단인 경우가 대부분 → 교환이 맞는 경우가 대부분. 이면 사진이 없다는 이유만으로 협의로 미루지 않음 (v3.7). 후미·전면추돌 범퍼는 표면이 가벼워도 사이드마운팅브라켓·체결탭·펜더 접합부 벌어짐을 확인 (adj2.1).
- 백도어 등 저가 패널은 교환안이 수리안과 비슷하거나 싼 경우가 많음 → 비용 비교에 반영 (v3.3).
- 경미손상 1~3유형은 적용대상 외판(앞뒤범퍼·후드·프런트펜더·도어·리어펜더·트렁크리드/백도어)에만. 그 외 부품은 정비 지식으로 직접 판단하고 damage_type "비대상(교환예외)".

## 시간 판단
- 판금·복원수리 시간만 과다 여부 판단. 탈착·도장공정·가열건조는 AOS 정시표 표준값이라 시간 자체는 판단하지 않음(작업유형의 정당성만) (v1.8).
- 작업유형이 정당화되지 않으면(수리로 충분한 손상에 교환 청구) 그 시간이 표준값이라도 "적정"이라 하지 않음 (v1.7).
- 판독 축(면적·프레스라인·꺾임·패임·이면 접근성·재질) → 소/중/대손상 등급 → 참고자료 시간표 → 없으면 숙련 판금공 시각으로 추정 (v3.0).

## 부수작업·중복
- 필수/선택 축 (adj2.6): 메인 작업에 정비 절차상 반드시 수반되는 작업은 사진 없어도 인정. "필요성은 있는데 사진이 없다"로 협의·불인정 금지. 선택 작업은 근거 없으면 협의.
- 교환·절단 패널에 체결된 소형 부속(에어익스트렉터그릴·몰딩·클립류·접착식 부품)은 메인 교환 인정 시 파손 사진 없어도 인정 (adj2.1, adj2.6).
- 탈착 중복은 "같은 부품 자체의 탈착"만 대상. 인접 부품 탈착은 정상 부수작업 (adj2.2, 106건 중 104건 조정 사례).
- O/H 포함 부속품 분할 청구, 동일 부품 탈착 1회, 동시탈착 한쪽 공임은 실제 기준.
- 사진에 작업 흔적이 있고 청구됐다는 사실만으로 인정 근거가 되지는 않음(손상·수리 범위와 무관한 작업).

## 근거 없는 청구
- 내판 판금은 양방향 (adj2.2): 변형·절단·용접·인장이 어느 사진에도 없으면 불인정, 보이면 범퍼 표면 인상과 무관하게 인정.
- 손해사정 기본값은 인정. 독립 판정 절반 이상이 불인정이면 기준 오적용으로 보고 재검토 (adj2.2).

## 단계별 차이
- 선견적(수리 전)은 작업 증빙(조색·이면·신품 장착 사진 등)을 요구하지 않음. 손상 대비 작업 종류·범위 타당성만 (v3.7).
- 손해사정(수리 후)은 새 손상 추론 금지, 청구-작업 대사 + 과잉수리 별도 확인 (adj1.1).
- 정비공정(견적 전)은 숨은 손상을 충격 강도에 비례해 추론, 램프류는 프레임에 보이면 무조건 항목화, 좌/우는 view_cue로 코드가 환산 (p2.6).

## 출력 형식
- "회사 기준 미제공 / AOS 확인 필요" 같은 군더더기 문구 금지, 정비 판단 결론까지만 (v3.8/adj2.7).
- 같은 작업이 두 곳(parts와 ancillary_work_check)에서 다른 판정 금지 (v3.10).
- 손해사정 종합의견 없음(항목별 판정이 결과). 선견적·정비공정은 종합의견 유지 (adj2.4).
- 교환도장·부품은 메인 판정에 연동(follows_parent). 메인 교환 불인정 시 교환도장은 "보수도장으로 변경"이지 과다청구가 아님 (adj2.1).
- photo_refs 필수, line_no는 견적서 NO(실험은 항목표 NO).

## 3008 측면 충돌 건에서 잡은 것 (v4.5/adj3.4)
- 같은 부위(group) 항목은 묶어서 본다: 도어 모듈판넬·트림 탈착이 인정된 부위에서 래치·윈도우모터·컨트롤모듈·배선·스피커를 개별 청구하면 O/H 분할 중복. "모듈판넬 분해에 수반"은 인정 사유가 아니라 중복 사유.
- 도장 등급과 탈착 범위 교차 확인: Lv2(패널 부착 외측만)인데 도어 탈착 + 내부 전체 분해면 서로 안 맞음. 이면 접근이 필요한 꺾임이면 분해 인정·Lv2 유지, 외측 손상뿐이면 분해가 과다.
- 한도 초과 0.1H 미만은 과다청구가 아니라 인정 + note (3.5H vs 3.45H 같은 0.05H 조정은 노이즈).

## K7 하이브리드 전후면 동시 충돌 건에서 잡은 것 (v4.6/adj3.5)
- 인접패널 블렌딩(보카시)은 인정하지 않음. 손상 없는 필러·도어에 "표면보수도장" 청구는 블렌딩이므로 불인정 (AI가 불인정한 게 맞았음). 컬러매칭(조색)은 별개.
- 재사용 가능 부품(혼·버저·VESS·센서·블로어·호스 등) 교환: 탈거품·신품 사진은 "했다"의 증거일 뿐. 파손 사진 없으면 파손 정도·장착 위치로 추론 — 충격 직접 전달 부위 + 주변 구조물 교환·변형이면 추론 인정, 가장자리·경미하면 협의 + 구품 파손 사진, 경로 밖이면 불인정. 사진 요구만 하지 말고 추론할 것.
- 판금 시간 큰 구조부(사이드멤버·휠하우스)는 그 면의 외판 밀림 깊이와 비례 대조. 전·후면 동시 손상은 면마다 따로. 반반이면 협의 + 계측자료.
- 정합성 경고는 촬영 시점 차이도 그대로 두고 담당자가 메모로 처리(3중 추돌 등).
- 한도 초과 0.1H "이하" 인정 + note.

## 출력 형식 정리 (v4.8/adj3.7/p3.2) — 판정 기준 변경 없음
- 출력 JSON 키를 축약형으로 보내고 서버에서 원래 키로 복원(wire-keys.ts). 화면·리포트는 원래 키만 봄.
- 손해사정 parent_line 필드 제거(어디서도 안 읽음). group·role·follows_parent는 유지.
- reasoning 상한: 인정은 한 문장, 그 외 두 문장 이내. adjustment_note·note·required_action 한 문장.
- 관리자 "AI 모델" 탭에 출력 상세도(상세/간략) 추가. 간략은 판정·항목 수·photo_refs·직접확인/추론 구분을 그대로 두고 문장만 구절로 압축. 실행 이력 태그에 -brief.
- 상세/간략 전체 검토 후 정리: 손해사정 "인정 + note" 모순은 adjustment_note 한도 메모 예외로 해소. 간략 모드 예외 — 판금·수리 항목은 판독 축 한 줄 유지, overall_opinion은 발신 형식 유지(항목당 한 문장), overall_summary 두 문장. 공통 블록 협의 용어를 도구별(협의대상/협의필요)로 병기. 정비공정은 판금 블록의 판독·등급(1~2단계)만 사용(견적서 대조 단계 제외).
- 간략 모드 첫 실측(모하비 133항목, 상세 1,143원 → 간략 981원): 현가 부품 8건이 일제히 추론 인정 → 협의로 쏠림. 원인은 "인정 15자 / 그 외 한 문장"의 비대칭(추론 경로를 쓸 수 있는 협의로 흘러감). 15자 규칙은 직접확인 인정에만, 추론 항목은 판정 무관 한 문장, photo_refs 줄이지 말 것으로 수정. 경계 항목 4건(헤드램프·사이드스텝·휠·TPMS)과 photo_evidence 직접/간접 25건은 실행 편차.

## 모하비 캐빈 탈거 건에서 잡은 것 (adj3.7 공통 블록 C)
- 부수작업 중복은 "메인 공임에 그 부속 분리가 포함되는가"로 판단. 메인 작업의 종류와 청구 시간을 먼저 본다.
  - 교환·O/H 공임 = 붙은 부속 분리 포함(범퍼커버 교환 2.69H에 커버·주간주행등·배선·업소버·립·포그램프 등 전부).
  - 단순 탈착 공임 = 통째로 들어내는 시간(범퍼 탈착 0.78H, 콘덴샤 탈착 0.35H). 부속을 실제로 분리했으면 별도 인정.
  - 참고자료의 "탈착 시 팬 공임 미적용"은 팬을 붙인 채 들어낸 경우. 분리 작업 사진이 있으면 별도.
- 간략 모드 수정 후 재실행: 현가 8건 인정으로 복귀, photo_refs 3.05로 회복. 남은 편차는 필러(과다↔인정)·프레임 17H(협의↔과다)·엔진마운트(중복 논리) — 엔진마운트 "엔진 탈착과 중복"은 위 규칙으로 해소 대상(교환은 파손 추론으로 판단, 탈착과 중복 아님).

## 손해사정 강도 3단계 (관리자 설정, 손해사정에만)
- 3단계 엄격 = 현재 프롬프트 그대로(블록 없음). 2·1단계는 프롬프트 뒤에 문턱 블록이 붙음. 판단 절차·근거 구분·중복·수량 판단은 단계와 무관.
- 2단계 표준: 추론 인정 문턱 "충분"→"상당(60%)", 작업 사진 있으면 반반도 인정, 외판은 관찰 포인트 없어도 면적 40%·다면·프레스라인 길게면 교환 인정, 한도 초과 0.3H까지 인정+메모.
- 1단계 관대: 작업 사진 있고 충격 경로와 무관하지 않으면 인정, 외판은 손상 보이고 시공됐으면 교환 인정(1~2유형 명백한 경우만 과다), 한도 초과 0.5H까지, 협의는 사진 요청 필요 건만.
- 실행 이력 태그 -s1/-s2로 구분.

## 보험개발원 경미손상 수리기준 반영 (공통 블록 A)
- 근거 명시: 표준약관 제21조 4항·별표 2. 적용대상을 기준 그대로 9개(앞·뒤범퍼, 후드, 프런트펜더, 프런트도어, 리어도어, 리어펜더, 트렁크리드, 백도어).
- 관찰 포인트 추가: 몰딩 부착 프레스 가공부 손상, 노후 부식 부위 손상, 부품별 교환 조건(도어 임팩트바 / 펜더 도어 인접부 꺾임 / 후드 스킨·내판 이탈 / 리어펜더 램프 모서리 / 트렁크리드 백피니셔 모서리 / 백도어 뒷유리 모서리), 범퍼 내부 브라켓·레일 파손, 같은 부위 이전 복원수리 흔적.
- 판단 구간: 복원 불가 손상을 공업사가 수리로 청구·시공했으면 교환으로 올리지 않고 수리 인정(하자 우려만 메모). 복원수리비 > 교환비가 명백하면 협의 아니라 교환 인정(별표 2 단서·FAQ).
- 수리방법: 1유형 폴리싱·범퍼 미탈착, 2유형 서페이서·베이스·클리어(범퍼 탈착은 이면 접근 필요 시), 3유형 외판 판금+방청+퍼티+도장 / 범퍼 퍼티·샌딩·열가공, 기타손상 범퍼 탈착·패치 복원.
- 품질인증부품 관련 문구는 실무상 쓰지 않아 전부 제거.

## adj4.0 — 판단 단위 재구성: 1단계 정비사(필요/불필요) → 2단계 손해사정사(적정성)
- 계기: 봉고 캡 전면 좌측 대파 건. 손상 중심 프롬프트가 탈착·O/H 항목에 "이 부품 파손 근거 없음 → 불인정"을 냄. 부수작업의 필요성은 파손이 아니라 메인 작업의 접근·분해 경로에서 나옴.
- 1단계: 파손 판독(외부 직접확인, 내부는 내부 수리 전 사진 또는 추론) → 수리 계획(차종 구조 → 메인 작업 → 접근·분해 경로) → 청구 항목을 필요/불필요로만 가림. 수준(교환 vs 판금)·시간·등급·중복은 1단계에서 판단하지 않음(관대해지지도 조이지도 않음, 사회통념상 통상 정비 범위). 불필요 = 경로 밖·사고 무관·설명 안 되는 내판.
- 2단계: 성립 항목만 수준(과잉수리 ①②③, 재사용 부품 교환은 파손 근거·추론)·실시(photo_evidence)·시간·수량·중복·도장·정합성. 관리자 강도는 2단계 문턱.
- 출력에 repair_plan(vehicle_structure, damage_summary, main_works, access_path, rejected)을 먼저 확정. rejected는 items에서 반드시 불인정. 화면·보고서에 "수리 계획" 패널.
- 기존 결정은 전부 유지(교환 3구간, 연동, 소형 부속, 재사용 부품 추론, 시간 한도 0.1H, 중복은 메인 공임 종류·시간, 블렌딩 불인정, 기본값 인정·절반 이상 불인정이면 재검토).
- 사용자공임·사용자부품(항목표 NO에 U): AOS 표준 항목이 아닌 공업사 직접 입력. 항목명·금액이 실제 무슨 작업인지 먼저 해석(부품란의 판금 공임 등), 표준 항목 기준으로 환산해 적정성 판단, 해석 불가면 협의 + 소명, 이중이면 중복. 항목표 구분에 "사용자공임/사용자부품"으로 표시, 화면에 "사용자" 배지.
- adj4.1: cost_comparison(교환안 vs 수리안 금액 비교) 출력 제거 — 금액이 정확히 비교되지 않아 불필요. 비용 비교 협의 구간은 유지하되 adjustment_note에 수리안(판금시간·도장 등급)만 한 줄, 금액은 쓰지 않음. 예전 결과는 화면에서 그대로 표시.

## 선견적 v5.0 — 손해사정 adj4.0과 같은 2단계 구조
- 1단계 정비사: 파손 판독(외부 직접확인, 내부는 추론) → 수리 계획(차종 구조·메인 작업·접근 경로) → 청구 항목 필요/불필요. 선견적 미제공이어도 계획은 세움(rejected 빈 배열).
- 2단계 손해사정사: 수준(복원 불가 → 교환 인정가능 / 복원 가능에 교환 → 불인정 + 대체 작업 / 저가 패널 → 협의대상, 금액 없이 수리안), parts 트리, 시간 두 축(한도 0.1H 이하 인정+note), 부수작업 두 축(mechanically_plausible = 경로 여부), 중복·수량·도장.
- 작업 전 단계 규칙(작업 증빙 요구 금지, 실시 여부 문구)과 회신문 형식·concerns·other_findings·분기는 유지. REPAIR_PLAN_SCHEMA를 공용으로 옮기고 rejected.line_no는 null 허용.

## 출력 군더더기 제거 (v5.1 / adj4.2) — 판정 기준 변경 없음
- 계기: 포터 15항목 선견적 출력 8.3k. labor_time_check를 15항목 전부 채움, ancillary_work_check에 parts 항목 되풀이(규칙 위반), concerns가 parts 요약, other_findings가 인정가능 재진술, "실시 여부는 손해사정 시 확인" 반복.
- 선견적: labor_time_check는 판금·복원수리만(그 외 null), ancillary_work_check는 청구서에 없는 부수작업만(parts 항목 금지), concerns 제거(appropriate만), other_findings는 문제 도메인만, required_action은 인정가능이면 빈 문자열, 실시 여부 문구 삭제, reasoning 인정가능 한 구절(15자)·그 외 한 문장.
- 공통: repair_plan.access_path는 항목+대상 작업만(이유 문장 제거). 손해사정 인정 reasoning 한 구절.

## v5.2 항목별 판정만 출력 — 되돌림
- 항목별 검토의견을 걷어내고 종합의견만 남기는 v5.2를 시험했으나 비용 차이가 거의 없어(추론 토큰이 지배) 되돌림. 현재 선견적 v5.1 / 손해사정 adj4.2 유지.

## 경미손상판독기 신설 (m1.0)
- 목적: 외판 한두 부위 사진만으로 경미손상 유형(1·2·3유형/기타손상)과 기준상 수리방법을 공식 근거(표준약관 별표 2·보험개발원 경미손상 수리기준)로 설명. 거래처·고객 회신문이 산출물.
- 범위: 청구·공임·시간·공정·과실 언급 없음. 공통 블록은 A(손상 판독과 수리·교환 판단)만 쓰고, 보험개발원 원문 요지(유형 정의·수리기준·교환 조건 공통 5 + 부품별)를 고정 블록으로 붙임. 품질인증부품은 배제.
- 입력: 도해도 클릭(9부품, 좌/우, 복수 선택, 세단 트렁크리드/SUV 백도어 토글) + 사진(상한 없음) + 선택 차량정보. 출력은 부위별로 관찰·유형·근거 확신·판정 근거·교환 조건 대조(해당/해당 없음/확인 불가)·수리방법·추가 사진·회신문.
- 규칙: 교환 조건 "해당"이 하나라도 있으면 기타손상. 사진으로 볼 수 없는 것(임팩트 바, 내측 패널 이탈)은 확인 불가. 도장막이 멀쩡해도 소재 변형이면 3유형. 판별 불가면 판독불가 + 필요한 각도 요청.
- 출력 상세도 설정과 무관하게 항상 상세(회신문 자체가 짧음). 강도 설정은 적용 안 함(청구 적정성 판단이 아님). 프롬프트 약 5k 토큰, 추론 medium.

## 경미손상 m1.1 — 판정 문장 축약, 트렁크/백도어 통합 (판정 기준 변경 없음)
- 싼타페 뒤범퍼·리어펜더 건: 판단은 맞았으나 판정 근거·조건 대조·수리방법이 서술형으로 길었음. 회신문만 문어체 4~6문장으로 두고 나머지는 구절로 제한(판정 근거 40자 "핵심 관찰 → 결론", 조건 이름·근거 15자, 수리방법 공정 "→" 연결, 평가 문구 금지).
- part_name에 "(좌)"를 붙여 side와 겹치던 것 → 이름만 쓰게 하고 코드에서도 떼어냄.
- 도해도 뒷면은 "트렁크/백도어" 하나로 두고, 세단·SUV 구분은 모델이 사진으로 판별해 결과 이름에 씀.
- 카드에 손상 깊이 단면(투명막·도장막·소재)과 유형 단계(1→2→3→기타) 그림 추가. 유형에서 바로 그리므로 출력 토큰 증가 없음.

## 약도그림판 신설 (AI 미사용)
- 사고 약도를 도로·물체·표시 부품으로 그려 PNG로 저장하는 도구. 홈 타일 `/sketch`. OpenAI 호출·서버 저장 없음(브라우저 localStorage "carshare-sketch"), 실행이력·사용량엔 안 잡힘.
- 구현: `src/lib/sketch-editor.js`(순수 DOM/SVG 단일 모듈, 의존성 0) + `(app)/sketch/page.tsx`가 마운트. 프로토타입(단일 HTML)에서 현업 검토 거친 뒤 그대로 옮김.
- 부품: 도로(직선 1~12차로·양방향/일방·중앙선·차로별 바닥 표기·버스전용, 교차로·T자로 팔마다 길이·왕복 2~12차로·접근 차로 표기, 회전교차로 회전 차로 1~4·진입 도로 차로, 곡선·횡단보도·정지선·안전지대·어린이 보호구역·방지턱·인도·주차구획) / 물체(승용·화물·버스·이륜·보행자, 자차/대물N 자동 번호, 가드레일·신호등 상태·표지판·정류장·건물·가로수·시야 차단) / 표시(화살표+라벨, 충돌 지점 순번, 스키드, 정지 위치, 치수, 번호, 방위, 텍스트, 파편).
- 규칙: 세로 기본(진행 위쪽, 우측통행), 층 고정(도로<노면<물체<표시), 고정 형태 객체는 크기 슬라이더·회전만, 늘리는 객체는 네 변 손잡이(잡은 쪽만). 1100px 이하는 팔레트 상단 스트립 + 속성 서랍, 핀치 줌.
- 남은 것: 도로 끝점 스냅, 차량 도로 방향 자동 정렬, 자유 펜, 다중 선택, 건별 탭, PDF 보고서, 손해사정·선견적 보고서 삽입.
